
{{ image }}_pulled:
  docker.pulled:
    - name: {{ repo ~ '/' if repo is defined else '' }}{{ image }}
    - tag: {{ tag }}
    - force: {{ force_pull if force_pull is defined else tag == 'latest' }}
