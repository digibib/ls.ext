export default {
  backendUri: '/services',
  maxVisibleFilterItems: 5,
  searchQuerySize: 10,
  filterableFields: [ 'work.publications.branches', 'work.publications.formats', 'work.publications.languages', 'work.publications.audiences' ]
}
