<!DOCTYPE html>
<html lang="no">
  <head>
    <meta charset="UTF-8" />
    <title>{{deichman:title}} av {{deichman:creator}} ({{deichman:date}})</title>
  </head>
  <body>
    <h1><span data-automation-id="work_title">{{deichman:title}}</span> / <span data-automation-id="work_author">{{deichman:creator}}</span> <span>(<span data-automation-id="work_date">{{deichman:date}}</span>)</span></h1>
    {{#items.length}}
    <h2>Vi har:</h2>
    <ul>
      {{#items}}
        <li><span data-automation-id="item_location">{{deichman:location}}</span> | <span data-automation-id="item_status">{{deichman:status}}</span></li>
      {{/items}}
    </ul>
    {{/items.length}}
    {{^items}}
    <h2>Vi har ingen eksemplarer</h2>
    {{/items}}
  </body>
</html>