<!DOCTYPE html>
<html lang="no">
  <head>
    <meta charset="UTF-8" />
    <title>{{title}} av {{creator}} ({{date}})</title>
  </head>
  <body>
    <h1>{{title}} av {{creator}} ({{date}})</h1>
    {{#hasEditions.length}}
    <h2>Vi har:</h2>
    <ul>
      {{#hasEditions}}
        <li><a href="{{.}}">{{.}}</a></li>
      {{/hasEditions}}
    </ul>
    {{/hasEditions.length}}
    {{^hasEditions.length}}
    {{#hasEditions}}
    <h2>Vi har</h2>
    <ul>
        <li><a href="{{hasEditions}}">{{hasEditions}}</a></li>
    </ul>
    {{/hasEditions}}
    {{^hasEditions}}
    <h2>Vi har ingen eksemplarer</h2>
    {{/hasEditions}}
    {{/hasEditions.length}}
  </body>
</html>